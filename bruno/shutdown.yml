info:
  name: Shutdown Server
  type: http
  seq: 9
  tags:
    - dangerous

http:
  method: POST
  url: "{{baseUrl}}/shutdown"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
