info:
  name: Nuke Sessions
  type: http
  seq: 8
  tags:
    - sessions
    - dangerous

http:
  method: POST
  url: "{{baseUrl}}/sessions/nuke"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: "{}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
