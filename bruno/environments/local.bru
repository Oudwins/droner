vars {
  baseUrl: http://localhost:57876
  # Default to the parent directory of this collection. When you run `bru` from
  # the bruno/ directory, `..` will resolve to the repo root (the droner codebase).
  # Override by setting DRONER_REPO_PATH in the environment if needed.
  dronerRepoPath: ../
}
