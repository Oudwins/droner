info:
  name: List Sessions
  type: http
  seq: 11
  tags:
    - sessions

http:
  method: GET
  url: "{{baseUrl}}/sessions"
  params:
    - name: limit
      value: "10"
      type: query
    - name: status
      value: queued
      type: query
    - name: status
      value: running
      type: query

settings:
  encodeUrl: true
